# Changelog
## 0.2.0
* Adds volume syncing through libpulse binding. Gated behind a feature flag but enabled by default (use `--no-default-features` to disable it)
## 0.1.0
* Initial release
* Supports time syncing to media dock